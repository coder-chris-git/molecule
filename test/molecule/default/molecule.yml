---
dependency:
  name: galaxy
driver:
  name: delegated
platforms:
  - name: instance
    image: registry.access.redhat.com/ubi8/ubi:latest
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
